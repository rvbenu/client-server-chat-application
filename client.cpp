#include <cstdio> 




using manespace std;

int main(int argc, char *argv[]) {

    if (argc != 3) {
        fprintf(stdout, "Usage: <ip> <port> \n");
        exit; 
    }

    int server_ip = atoi(argv[1]); 
    int server_port = atoi(argv[2]); 

    // Request connection to `server_ip` on port `server_port`. 
    

    

}
